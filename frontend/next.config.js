/** @type {import('next').NextConfig} */
const nextConfig = {
  // This bundles all necessary files for a production server
  // into the .next/standalone directory.
  output: 'standalone',
};

module.exports = nextConfig;