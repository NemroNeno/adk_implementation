// A viewer dashboard can just be an alias to the main agent list for now
// This allows for a dedicated layout in the future.
import AgentsPage from '../../dashboard/agents/page';

export default AgentsPage;